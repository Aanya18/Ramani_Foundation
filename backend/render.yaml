services:
  - type: web
    name: ramani-foundation-api
    runtime: python
    pythonVersion: "3.11"
    
    # Build configuration
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1"
    
    # Environment variables
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: ENVIRONMENT
        value: "production"
      - key: PORT
        value: "10000"
    
    # Health check
    healthCheckPath: "/"
    healthCheckStartTimeout: 300
    
    # Auto-deploy
    autoDeploy: true
